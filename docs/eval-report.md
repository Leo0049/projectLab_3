# Evaluation report

## Suite A — deterministic governance tests

Generated by `mvn verify`. These gate the build: any failure is a build failure.

| Category | Cases | Status |
|---|---|---|
| Tool-level RBAC (tool × role matrix) | 26 | pass |
| Tenant isolation | 8 | pass |
| Masking, asserted on the wire payload | 8 | pass |
| Audit fail-closed and transaction boundary | 6 | pass |
| Audit write failure refuses the call | 1 | pass |
| Approval state machine and idempotency | 7 | pass |
| Rate limiting | 4 | pass |
| Startup validation | 5 | pass |
| Error-message contract | 6 | pass |
| Tool discovery over the protocol | 2 | pass |
| Spike 1 regression (AOP proxy vs discovery) | 2 | pass |
| Eval dataset validation | 6 | pass |
| Migration and seed smoke test | 1 | pass |
| **Total** | **82** | **100% pass** |

Reproduce with:

```bash
mvn verify
```

## Suite B — LLM evaluation

**Not yet run. No scores are recorded.**

The dataset (30 questions), the scorer and its unit tests exist and are
verified; the driver that puts the questions to a model has not been built, so
there is nothing to report. See `docs/eval/README.md` for what remains.

The numbers below are deliberately blank. Filling them in with plausible
figures would be inventing data, and the first question anyone asks about a
metric is how it was measured.

| Metric | Definition | Target | Measured |
|---|---|---|---|
| Tool selection accuracy | correct tool calls / questions expecting a call (27) | ≥ 90% | — |
| Argument accuracy | fully correct arguments / calls with expected arguments | ≥ 85% | — |
| Abstention accuracy | unanswerable questions with no tool call (3) | 100% | — |
| Isolation held under injection prompts | no cross-tenant data in any response | 100% | proven by Suite A |

The last row is the one that matters for security, and it does not depend on
this suite: tenant isolation is asserted deterministically in Suite A on every
push. Suite B would only cross-check it.

---

*Suite A figures above are from the local run of the commit that introduced
them. Re-run `mvn verify` to regenerate.*
